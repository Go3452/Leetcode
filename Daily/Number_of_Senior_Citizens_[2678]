class Solution {
    public int countSeniors(String[] details) {
        int n=details.length;
        int count=0;
        for(int i=0;i<n;i++)
        {
         char ch=details[i].charAt(11);
         if(ch=='6')
         {
          char ch1=details[i].charAt(12);
          if(ch1>'0'&&ch1<='9')
           count++;
         }
         else if(ch>'6')
          count++;
        }
        return count;
    }
}

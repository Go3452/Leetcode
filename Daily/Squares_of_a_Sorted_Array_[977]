class Solution {
    public int[] sortedSquares(int[] nums) {
        int n=nums.length;
        for(int i=0;i<n;i++)
        {
         int num=nums[i];
         nums[i]=num*num;
        }
        Arrays.sort(nums);
        return nums;
    }
}

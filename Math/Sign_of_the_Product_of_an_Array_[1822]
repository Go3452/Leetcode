class Solution {
    public int arraySign(int[] nums) {
        int neg=0,n=nums.length;
        for(int i=0;i<n;i++)
        {
         if(nums[i]==0)
          return 0;
         else if(nums[i]<0)
          neg++;
        }
        if(neg%2==1)
         return -1;
        return 1;
    }
}

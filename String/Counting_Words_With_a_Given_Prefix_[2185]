class Solution {
    public int prefixCount(String[] words, String pref) {
        int n=words.length,count=0;
        for(int i=0;i<n;i++)
        {
         String word=words[i];
         int ind=word.indexOf(pref);
         if(ind==0) count++;
        }
        return count;
    }
}

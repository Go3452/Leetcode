class Solution {
    public int[] shuffle(int[] nums, int n) {
        int arr[] = new int[2*n];
        for(int i=0,j=n,k=0;i<n;i++,j++)
        {
         arr[k]=nums[i];k++;
         arr[k]=nums[j];k++;
        }
        return arr;
    }
}
